{%- style -%}
  #shopify-section-{{ section.id }} {
    --st-navy: {{ section.settings.navy_color }};
    --st-line: {{ section.settings.line_color }};
  }
{%- endstyle -%}

<style>
  #shopify-section-{{ section.id }} * { box-sizing: border-box; }

  #shopify-section-{{ section.id }} .stats {
    background: {{ section.settings.bg_color }};
    padding-block: clamp(24px, 3vw, 40px);
    padding-inline: clamp(16px, 4vw, 40px);
  }

  #shopify-section-{{ section.id }} .stats__inner {
    max-width: 1240px;
    margin-inline: auto;
    border-bottom: 1px solid var(--st-line);
  }

  #shopify-section-{{ section.id }} .stats__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
  }

  #shopify-section-{{ section.id }} .stats__item {
    padding: clamp(20px, 2.4vw, 30px) clamp(10px, 1.6vw, 20px);
    text-align: center;
    border-left: 1px solid var(--st-line);
  }

  #shopify-section-{{ section.id }} .stats__num {
    margin: 0 0 12px;
    font-family: var(--font-{{ section.settings.heading_font }}-family);
    font-weight: 800;
    font-size: clamp(28px, 3.2vw, 40px);
    line-height: 1;
    letter-spacing: -0.5px;
    color: var(--st-navy);
  }

  #shopify-section-{{ section.id }} .stats__label {
    margin: 0;
    font-family: var(--font-{{ section.settings.mono_font }}-family);
    font-weight: 400;
    font-size: clamp(12px, 1.1vw, 13px);
    letter-spacing: 0.8px;
    text-transform: uppercase;
    line-height: 1.4;
    color: var(--st-navy);
  }

  #shopify-section-{{ section.id }} .stats__item:nth-child(4n + 1) { border-left: none; }

  @media (max-width: 720px) {
    #shopify-section-{{ section.id }} .stats__grid { grid-template-columns: repeat(2, 1fr); }
    #shopify-section-{{ section.id }} .stats__item:nth-child(n)      { border-left: 1px solid var(--st-line); border-top: none; }
    #shopify-section-{{ section.id }} .stats__item:nth-child(2n + 1) { border-left: none; }
    #shopify-section-{{ section.id }} .stats__item:nth-child(n + 3)  { border-top: 1px solid var(--st-line); }
  }
</style>

<div class="stats">
  <div class="stats__inner">
    <div class="stats__grid">
      {%- for block in section.blocks -%}
        {%- if block.type == 'stat' -%}
          <div class="stats__item" {{ block.shopify_attributes }}>
            <p class="stats__num">{{ block.settings.number | escape }}</p>
            <p class="stats__label">{{ block.settings.label | escape }}</p>
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Community Stats",
  "tag": "section",
  "class": "community-stats-section",
  "max_blocks": 8,
  "settings": [
    {
      "type": "header",
      "content": "Fonts"
    },
    {
      "type": "select",
      "id": "heading_font",
      "label": "Number font",
      "options": [
        { "value": "tag",     "label": "PP Neue Montreal Mono" },
        { "value": "heading", "label": "D-DIN" },
        { "value": "accent",  "label": "Alkaline Test" }
      ],
      "default": "heading"
    },
    {
      "type": "select",
      "id": "mono_font",
      "label": "Label font",
      "options": [
        { "value": "tag",     "label": "PP Neue Montreal Mono" },
        { "value": "heading", "label": "D-DIN" },
        { "value": "accent",  "label": "Alkaline Test" }
      ],
      "default": "tag"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "navy_color",
      "label": "Number & label color",
      "default": "#2b3054"
    },
    {
      "type": "color",
      "id": "line_color",
      "label": "Divider color",
      "default": "#e9e9ec"
    }
  ],
  "blocks": [
    {
      "type": "stat",
      "name": "Stat",
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "Number",
          "default": "50K+"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Members"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Community Stats",
      "blocks": [
        { "type": "stat", "settings": { "number": "50K+",  "label": "Members" } },
        { "type": "stat", "settings": { "number": "8",     "label": "Forum Categories" } },
        { "type": "stat", "settings": { "number": "12+",   "label": "AMA Sessions Hosted" } },
        { "type": "stat", "settings": { "number": "40+",   "label": "Countries Represented" } }
      ]
    }
  ]
}
{% endschema %}
